/**
 * Exercise type identifiers — used in lesson config and UI routing (TSH-003).
 * Keep values stable; they are the contract between data and components.
 */
export const CARD_TYPE = {
  LISTEN: 'listen',
  REPEAT: 'repeat',
  CHOOSE: 'choose',
}

/** @typedef {'listen' | 'repeat' | 'choose'} CardType */
